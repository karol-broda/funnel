---
title: installation
icon: DownloadSimpleIcon
description: how to install the funnel client and server.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Folder, Files } from 'fumadocs-ui/components/files';

get started with funnel by installing the client and running the server using docker.

## client installation

<Tabs items={['Quick Install', 'Manual Install', 'Build from Source']}>
<Tab value="Quick Install">
the easiest way to install the `funnel` client:

```bash
curl -LsSf https://raw.githubusercontent.com/karol-broda/funnel/master/scripts/install.sh | sh
```

supports linux and macos on both amd64 and arm64 architectures.
</Tab>

<Tab value="Manual Install">
download from the [releases page](https://github.com/karol-broda/funnel/releases) and install:

```bash
# example for linux amd64
sudo mv funnel /usr/local/bin/funnel
sudo chmod +x /usr/local/bin/funnel
```
</Tab>

<Tab value="Build from Source">
requires [go](https://go.dev/doc/install) 1.21 or later:

```bash
git clone https://github.com/karol-broda/funnel.git
cd funnel
make build-client
sudo mv ./bin/funnel /usr/local/bin/funnel
```
</Tab>
</Tabs>

## server installation

<Callout title="docker required" intent="warning">
the server runs as a docker container. you'll need [docker](https://docs.docker.com/get-docker/) installed.
</Callout>

### using docker image

```bash
# pull and verify the server image
docker pull ghcr.io/karol-broda/funnel-server:latest
docker run --rm ghcr.io/karol-broda/funnel-server:latest version
```

### building from source

```bash
git clone https://github.com/karol-broda/funnel.git
cd funnel
make build-server-docker
```

## project structure

<Files>
  <Folder name="funnel" defaultOpen>
    <Folder name="client" defaultOpen>
      <File name="client.go" />
      <File name="connection.go" />
      <File name="runner.go" />
    </Folder>
    <Folder name="server" defaultOpen>
      <File name="server.go" />
      <File name="router.go" />
      <File name="tunnel.go" />
    </Folder>
    <Folder name="cmd" defaultOpen>
      <Folder name="funnel" defaultOpen>
        <File name="main.go" />
      </Folder>
      <Folder name="server" defaultOpen>
        <File name="main.go" />
      </Folder>
    </Folder>
  </Folder>
</Files>

## next steps

**[basic usage](/docs/getting-started/basic-usage)** - create your first tunnel  
**[server deployment](/docs/server/deployment)** - deploy for production  
**[cli reference](/docs/reference/client-cli)** - explore all commands 